describe(Cypress.env('brand').toUpperCase() + ' - dummy', function () {

    before(function () {
        // To run at the beginning of a test spec, only once

    })

    beforeEach(function () {
        // To run before each test

    })

    afterEach(function () {
        // To run after each test

    })

    after(function () {
        // To run at the end of a test spec, only once

    })

    it('Dummy test case', function () {
        // Dedicated test run steps, including assertions
    })

})